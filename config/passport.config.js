// If you're no longer using Passport at all, you can remove this file entirely.
// But if you plan to keep Passport for other strategies later, you can retain this basic structure.

const passport = require('passport');

// If you are NOT using any strategy anymore (like local, jwt, etc), you can delete this whole file.

module.exports = passport;
